
#Область ПрограммныйИнтерфейс

Функция MarkdownToHTML(Знач ИсходныйТекст) Экспорт
	
	Шаблон = MarkdownEditorServerCached.GetHTMLTemplate();
	
	ИсходныйТекст = СтрЗаменить(ИсходныйТекст, Символы.ПС, "\r\n");
	ИсходныйТекст = СтрЗаменить(ИсходныйТекст, "'", "\'");
	
	Возврат СтрШаблон(Шаблон, ИсходныйТекст);
	
КонецФункции

#КонецОбласти